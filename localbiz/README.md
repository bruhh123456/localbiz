localbiz — helper folder

Files:
- html3.html — demo page showing images from the main `images/` folder
- html3 — same file without extension for compatibility

Open `localbiz/html3.html` in a browser to view the images (ensure `images/` exists in the parent folder).